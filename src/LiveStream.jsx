import React,{useMemo}from"react";
function getYouTubeId(value=""){const url=String(value).trim();if(!url)return"";const patterns=[/youtu\.be\/([A-Za-z0-9_-]{11})/,/[?&]v=([A-Za-z0-9_-]{11})/,/youtube\.com\/live\/([A-Za-z0-9_-]{11})/,/youtube\.com\/embed\/([A-Za-z0-9_-]{11})/];for(const pattern of patterns){const match=url.match(pattern);if(match)return match[1]}return""}
export default function LiveStream({liveStream}){const config=liveStream&&typeof liveStream==="object"?liveStream:{};const enabled=Boolean(config.enabled);const videoId=useMemo(()=>getYouTubeId(config.url||config.videoUrl),[config.url,config.videoUrl]);if(!enabled||!videoId)return null;const title=config.title||"Live Worship Service";const description=config.description||"Join us live for worship, the Word of God and fellowship.";return <section className="homeSection liveStreamSection" id="live"><div className="sectionIntro centered"><span className="liveBadge"><i/> LIVE NOW</span><h2>{title}</h2></div><p className="liveDescription">{description}</p><div className="livePlayer"><iframe title={title} src={`https://www.youtube.com/embed/${videoId}?autoplay=0&rel=0&modestbranding=1&playsinline=1`} allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowFullScreen/></div><div className="liveActions"><a href={`https://www.youtube.com/watch?v=${videoId}`} target="_blank" rel="noopener noreferrer">Watch on YouTube</a></div></section>}
