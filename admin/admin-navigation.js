(function(){
'use strict';
const ITEMS=[['identity','Church & Homepage','site.view','site.edit'],['about','About','site.view','site.edit'],['services','Services','site.view','site.edit'],['links','Homepage Links','site.view','site.edit'],['classes','Membership Classes','site.view','site.edit'],['mediaSettings','Media, Hero & Audio','media.view','media.edit'],['live','Live Streaming','live.view','live.edit'],['theme','Theme','theme.view','theme.edit'],['media','Media Library','media.view','media.edit'],['comments','Live Comments','comments.view','comments.moderate'],['adminRbac','Users & Permissions','users.view','users.edit']];
const SITE_IDS=new Set(['identity','about','services','links','classes','mediaSettings','live','theme']);
function permission(p){return window.AdminRBAC?.hasPermission?.(p)??false;}
function loadStyles(){if(document.querySelector('link[data-admin-navigation-css]'))return;const link=document.createElement('link');link.rel='stylesheet';link.href='/admin/admin-navigation.css';link.dataset.adminNavigationCss='true';document.head.appendChild(link);}
function updateOffsets(){const header=document.querySelector('.admin-header'),tabs=document.querySelector('.admin-navigation');const h=header?Math.ceil(header.getBoundingClientRect().height):56;if(header)document.documentElement.style.setProperty('--admin-header-offset',h+'px');if(tabs)document.documentElement.style.setProperty('--admin-tabs-offset',Math.ceil(tabs.getBoundingClientRect().height)+'px');}
function getViews(){const out={};ITEMS.forEach(([id,label,view,edit])=>out[id]={el:document.getElementById(id),label,view,edit});return out;}
function applySectionPermissions(){const all=getViews();ITEMS.forEach(([id,,view,edit])=>{const item=all[id];if(!item.el)return;const canView=permission(view),canEdit=permission(edit);item.el.classList.toggle('admin-view-hidden',!canView);if(!canEdit)item.el.querySelectorAll('input,textarea,select,button').forEach(control=>{if(control.closest('.head')||control.classList.contains('rbac-exempt'))return;(control.closest('label,.field,.form-row,.toolbar,.form-actions,.editor-actions')||control).classList.add('rbac-hidden');});});const jump=document.querySelector('#site > .jump');if(jump)jump.classList.add('rbac-hidden');}
function showView(name,button){const all=getViews(),target=all[name];if(!target?.el||!permission(target.view))return;Object.values(all).forEach(v=>{if(v.el)v.el.classList.add('admin-view-hidden');});target.el.classList.remove('admin-view-hidden');const site=document.getElementById('site');if(site)site.classList.toggle('admin-view-hidden',!SITE_IDS.has(name));const savebar=document.querySelector('#site > .savebar');if(savebar)savebar.classList.toggle('rbac-hidden',!SITE_IDS.has(name)||!permission('site.edit'));document.querySelectorAll('.admin-navigation [data-view]').forEach(b=>b.classList.toggle('active',b===button));if(name==='media'&&typeof window.loadMedia==='function')window.loadMedia();if(name==='comments'&&typeof window.loadComments==='function')window.loadComments();if(name==='adminRbac'&&window.AdminRBAC?.loadUsers)window.AdminRBAC.loadUsers();window.scrollTo({top:0,behavior:'smooth'});}
function applyVisibility(){const nav=document.querySelector('.admin-navigation');if(!nav)return;applySectionPermissions();nav.querySelectorAll('[data-view]').forEach(b=>b.classList.toggle('rbac-hidden',!permission(b.dataset.permission)));const active=nav.querySelector('button.active:not(.rbac-hidden)')||nav.querySelector('button:not(.rbac-hidden)');if(active)showView(active.dataset.view,active);}
function buildNavigation(){const existing=document.querySelector('.tabs');if(!existing)return;const nav=document.createElement('nav');nav.className='tabs admin-navigation';nav.setAttribute('aria-label','Admin sections');nav.innerHTML=ITEMS.map(([id,label,view])=>`<button type="button" data-view="${id}" data-permission="${view}">${label}</button>`).join('');existing.replaceWith(nav);nav.querySelectorAll('[data-view]').forEach(b=>b.addEventListener('click',()=>showView(b.dataset.view,b)));requestAnimationFrame(()=>{updateOffsets();applyVisibility();});if(window.ResizeObserver){const observer=new ResizeObserver(updateOffsets),header=document.querySelector('.admin-header');if(header)observer.observe(header);observer.observe(nav);}window.addEventListener('resize',updateOffsets,{passive:true);}
function init(){loadStyles();buildNavigation();}
window.AdminNavigation={showView,applyVisibility,applySectionPermissions,updateOffsets};
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();